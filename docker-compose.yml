version: '3'

services:

  frontend:
    container_name: "frontend"
    build:
      context: ./src/Front
      dockerfile: Dockerfile
    environment:
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "3000:3000"
    volumes:
      - /app/node_modules
      - ./front:/app/

  backend:
    container_name: "backend"
    build:
      context: ./src/Back
      dockerfile: Dockerfile
    env_file:
      - .env
    depends_on:
      - database
    environment:
        DATABASE_URL: mysqlx://root:crud1234@database:3306/nodeCrud
    volumes:
      - /app/node_modules
      - ./back:/app/

  nginx:
    depends_on:
      - frontend
      - backend
    restart: always
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "3050:80"

  database:
    container_name: "data_crudApp"
    image: "mysql"
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: "crud1234"
      MYSQL_DATABASE: "nodeCrud"
      MYSQL_USER: "nodecrud"
      MYSQL_PASSWORD: "crud1234"

volumes:
  dbdata: {}
